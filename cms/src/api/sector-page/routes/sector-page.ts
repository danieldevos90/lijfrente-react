/**
 * sector-page router
 * Routes registered for Content API access
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::sector-page.sector-page' as any);
